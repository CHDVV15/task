<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.SamStore"
        tools:targetApi="31">
        <activity android:name=".enter"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".buds"/>
        <activity android:name=".watch"/>
        <activity android:name=".tv"/>
        <activity android:name=".s23"/>
        <activity android:name=".pay"
            android:noHistory="true"/>
        <activity android:name=".list"/>
        <activity android:name=".f34"/>
        <activity android:name=".thank"
            android:noHistory="true"/>
    </application>

</manifest>